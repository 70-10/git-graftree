{
  "name": "git-graftree",
  "version": "0.1.0",
  "description": "Create git worktree and copy/symlink files from base directory",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "bin": {
    "git-graftree": "./dist/git-graftree"
  },
  "scripts": {
    "build": "bun build --compile --outfile=dist/git-graftree src/cli.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^24.0.10"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "citty": "^0.1.6"
  }
}
